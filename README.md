# WebShop - Полнофункциональный интернет-магазин

**🚀 Готовый к продакшену full-stack интернет-магазин с Django REST API бэкендом и React фронтендом**

## Описание проекта

WebShop - это современный интернет-магазин с полной интеграцией платежей через Stripe, системой аутентификации пользователей, корзиной покупок и управлением заказами. Проект полностью мигрирован с Django-шаблонов на React и готов к развертыванию.

## ✨ Возможности

- 🛒 **Каталог товаров** с категориями и поиском
- 👤 **Регистрация и аутентификация** пользователей
- 🛍️ **Корзина покупок** с управлением количеством
- 💳 **Интеграция Stripe** для оплаты заказов
- 📋 **Управление заказами** с историей покупок
- 👨‍💼 **Админ-панель** Django для управления контентом
- 📱 **Адаптивный дизайн** для всех устройств

## Структура проекта

```
WebShop/
├── shop/                    # Django REST API бэкенд
│   ├── api/                # DRF endpoints
│   ├── main/               # Модели Product/Category
│   ├── cart/               # Сессионная корзина
│   ├── users/              # Аутентификация и профили
│   ├── orders/             # Заказы и история
│   ├── payment/            # Stripe интеграция
│   ├── scripts/            # Утилиты
│   └── manage.py
├── frontend/               # React SPA фронтенд
│   ├── src/
│   │   ├── components/     # Переиспользуемые компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── contexts/      # React Context (Auth, Cart)
│   │   └── api.js         # API клиент
│   ├── dist/              # Production build
│   └── package.json
├── .env                    # Конфигурация (Stripe ключи)
└── README.md
```

## 🛠️ Технологии

### Бэкенд (Django)
- **Django 5.2** - веб-фреймворк
- **Django REST Framework** - API для React
- **PostgreSQL** - база данных
- **Stripe** - обработка платежей
- **django-cors-headers** - CORS для React
- **python-dotenv** - управление переменными окружения

### Фронтенд (React)
- **React 18** - пользовательский интерфейс
- **Vite 5** - сборщик и dev сервер
- **React Router** - навигация
- **Axios** - HTTP клиент
- **CSS Modules** - стилизация

## 🚀 Быстрый старт

### Предварительные требования
- **Python 3.8+**
- **Node.js 16+**
- **PostgreSQL** (или SQLite для разработки)
- **Stripe аккаунт** (для платежей)

### 1. Клонирование и установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd WebShop

# Настройка бэкенда
cd shop

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `shop/`:

```env
# Django настройки
DEBUG=True
SECRET_KEY=django-insecure-change-this-in-production-123456789
ALLOWED_HOSTS=localhost,127.0.0.1

# База данных (PostgreSQL или SQLite)
# Для SQLite используйте:
DATABASE_URL=sqlite:///db.sqlite3

# Для PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/webshop

# Stripe настройки (получите ключи из https://dashboard.stripe.com/test/apikeys)
STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY_HERE
STRIPE_SECRET_KEY=sk_test_YOUR_SECRET_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE
```

### 3. Инициализация базы данных

```bash
# Примените миграции
python manage.py migrate

# Создайте суперпользователя
python manage.py createsuperuser

# (Опционально) Загрузите тестовые данные
python manage.py loaddata fixtures/sample_data.json  # если есть
```

### 4. Настройка фронтенда

```bash
# В новом терминале
cd frontend

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

### 5. Запуск серверов

```bash
# Терминал 1 - Django бэкенд
cd shop
python manage.py runserver

# Терминал 2 - React фронтенд
cd frontend
npm run dev
```

**🌐 Приложение будет доступно:**
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **Admin panel:** http://localhost:8000/admin/

## ✅ Проверка работы

### Бэкенд (Django)
1. **Admin panel:** http://localhost:8000/admin/
2. **API endpoints:** http://localhost:8000/api/v1/
3. **API docs:** http://localhost:8000/api/v1/ (DRF browsable API)

### Фронтенд (React)
1. **Главная страница:** http://localhost:3000
2. **Каталог товаров:** http://localhost:3000/shop
3. **Корзина:** http://localhost:3000/cart
4. **Профиль:** http://localhost:3000/user/profile

### Тестовый сценарий
1. **Регистрация:** Создайте аккаунт
2. **Просмотр товаров:** Добавьте товары в корзину
3. **Оформление заказа:** Заполните данные и оплатите (тестовые данные Stripe)
4. **Проверка заказа:** Посмотрите историю заказов в профиле

**🎉 Если все работает - поздравляем! Интернет-магазин готов к использованию.**

## 📡 API Endpoints

### 🔍 Категории товаров
- `GET /api/v1/categories/` - список всех категорий
- `GET /api/v1/categories/{slug}/` - детали категории

### 🛒 Товары
- `GET /api/v1/products/` - список товаров с фильтрами
- `GET /api/v1/products/available/` - только доступные товары
- `GET /api/v1/products/{slug}/` - детали товара

### 👤 Пользователи и аутентификация
- `POST /api/v1/auth/register/` - регистрация пользователя
- `POST /api/v1/auth/login/` - вход в систему
- `POST /api/v1/auth/logout/` - выход из системы
- `GET /api/v1/auth/profile/` - профиль текущего пользователя
- `PUT /api/v1/auth/profile/` - обновление профиля

### 🛍️ Корзина
- `GET /api/v1/cart/` - содержимое корзины
- `POST /api/v1/cart/add/` - добавить товар в корзину
- `POST /api/v1/cart/update_quantity/` - изменить количество
- `POST /api/v1/cart/remove_item/` - удалить товар
- `GET /api/v1/cart/get_quantity/` - количество товаров в корзине
- `POST /api/v1/cart/clear/` - очистить корзину

### 📋 Заказы
- `GET /api/v1/orders/` - список заказов пользователя
- `GET /api/v1/orders/{id}/` - детали заказа
- `POST /api/v1/orders/` - создание заказа из корзины
- `POST /api/v1/orders/{id}/mark_paid/` - отметить заказ оплаченным

### 💳 Платежи (Stripe)
- `POST /api/v1/payment/create-checkout-session/{order_id}/` - создать сессию оплаты
- `POST /payment/webhook/` - webhook для подтверждения оплаты

### 🔍 Поиск и фильтры
- `GET /api/v1/search/` - поиск товаров
- `GET /api/v1/search/autocomplete/` - автодополнение поиска

## ⚙️ Настройки и конфигурация

### CORS и проксирование
- **CORS:** Настроен в `shop/shop/settings.py` для `localhost:3000` и `localhost:5173`
- **Прокси:** Vite автоматически проксирует `/api/*` на `localhost:8000`
- **Cookies:** Включены для сессий Django

### Переменные окружения
```env
DEBUG=True                          # Режим разработки
SECRET_KEY=your-secret-key         # Django secret key
DATABASE_URL=sqlite:///db.sqlite3   # Или PostgreSQL URL

# Stripe ключи (получите в https://dashboard.stripe.com/test/apikeys)
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
```

## 🚀 Сборка для продакшена

### Фронтенд
```bash
cd frontend
npm run build
```
Собранные файлы будут в `frontend/dist/`

### Бэкенд
```bash
cd shop
pip install gunicorn
gunicorn shop.wsgi:application --bind 0.0.0.0:8000
```

### Docker (опционально)
```dockerfile
# Создайте Dockerfile для полного развертывания
FROM python:3.11
COPY . /app
WORKDIR /app
RUN pip install -r requirements.txt
EXPOSE 8000
CMD ["gunicorn", "shop.wsgi:application", "--bind", "0.0.0.0:8000"]
```

## 🐛 Устранение неполадок

### Проблемы с запуском

#### "ModuleNotFoundError: No module named 'django'"
```bash
# Убедитесь что виртуальное окружение активировано
cd shop
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

#### "Connection refused" (база данных)
```bash
# Для SQLite - ничего не нужно
# Для PostgreSQL - проверьте настройки в .env
python manage.py dbshell  # тест подключения
```

### Проблемы с API

#### "CSRF Failed: CSRF token missing"
- Проверьте что пользователь авторизован
- Проверьте настройки CORS в Django
- Проверьте что cookies отправляются с запросами

#### "Authentication required"
- Авторизуйтесь через `/user/login`
- Проверьте токен в localStorage

### Проблемы с платежами

#### "Stripe keys not configured"
```bash
# Проверьте .env файл
cat shop/.env | grep STRIPE
```

#### Webhook не работает
```bash
# Для локальной разработки используйте Stripe CLI
stripe listen --forward-to localhost:8000/payment/webhook/

# Или настройте webhook в Stripe Dashboard на ngrok URL
```

#### Заказ не отмечается оплаченным
- Проверьте логи Django на ошибки webhook
- Проверьте статус в Stripe Dashboard
- Используйте резервный механизм `markPaid` API

### Проблемы с фронтендом

#### "Failed to fetch" (CORS)
```bash
# Проверьте что Django сервер запущен
curl http://localhost:8000/api/v1/products/

# Проверьте настройки CORS
```

#### Белый экран
- Проверьте консоль браузера (F12)
- Проверьте что API endpoints доступны
- Проверьте React роутинг

### Проверка состояния системы

```bash
# Бэкенд
cd shop && python manage.py check

# Фронтенд
cd frontend && npm run build

# База данных
cd shop && python manage.py dbshell --command="SELECT COUNT(*) FROM main_product;"

# Stripe
cd shop && python -c "import stripe; stripe.api_key='your_key'; print('OK')"
```

## 📚 Архитектура и особенности

### 🔄 Полностью мигрирован на React
Проект полностью мигрирован с Django-шаблонов на React SPA. Все страницы теперь являются React компонентами.

### 💳 Stripe интеграция
- **Checkout Sessions** для безопасной оплаты
- **Webhook обработка** для подтверждения платежей
- **Test mode** для разработки
- **Live mode** для продакшена

### 🛡️ Безопасность
- **CSRF защита** для API запросов
- **CORS** для кросс-доменных запросов
- **Session-based аутентификация**
- **Token-based API** для React

### 📱 Адаптивный дизайн
- **Mobile-first** подход
- **Bootstrap** для компонентов
- **Custom CSS** для уникального стиля
- **Responsive** навигация и формы

### 🚀 Производительность
- **Vite** для быстрой сборки
- **Code splitting** для оптимизации загрузки
- **Lazy loading** компонентов
- **Optimized bundles** для продакшена

## 📖 Документация проекта

### Основные файлы
- `MIGRATION_PLAN.md` - план миграции Django → React
- `MIGRATION_STATUS.md` - статус выполнения миграции
- `QUICKSTART.md` - быстрый старт для разработчиков
- `debug_payment_flow.md` - отладка платежей

### Структура кода

#### Бэкенд (`shop/`)
```
api/          # DRF endpoints и сериализаторы
main/         # Модели Product/Category
users/        # Аутентификация и профили
orders/       # Заказы и платежи
cart/         # Корзина покупок
payment/      # Stripe интеграция
scripts/      # Утилиты для управления данными
```

#### Фронтенд (`frontend/`)
```
components/   # Переиспользуемые компоненты (Header, Footer, etc.)
pages/        # Страницы приложения (Home, Products, Cart, etc.)
contexts/     # React Context (AuthContext, CartContext)
api.js        # HTTP клиент для API
```

### Ключевые компоненты
- **Header.jsx** - навигация и меню пользователя
- **HomePage.jsx** - главная страница с популярными товарами
- **ProductsPage.jsx** - каталог товаров с фильтрами
- **CartPage.jsx** - корзина покупок
- **CheckoutPage.jsx** - оформление заказа
- **OrderDetailPage.jsx** - детали заказа и оплата

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

### Требования к контрибьюции
- Следовать PEP 8 для Python кода
- Использовать ESLint для JavaScript
- Добавлять тесты для новых функций
- Обновлять документацию

## 📄 Лицензия

MIT License - см. файл `LICENSE` для подробностей.

## 🙏 Благодарности

- **Django** и **Django REST Framework** за отличный бэкенд фреймворк
- **React** и **Vite** за современный фронтенд стек
- **Stripe** за простую интеграцию платежей
- **Bootstrap** за готовые UI компоненты

---

**🎉 Проект готов к использованию и развитию!**



